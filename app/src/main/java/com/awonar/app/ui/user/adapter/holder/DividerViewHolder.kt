package com.awonar.app.ui.user.adapter.holder

import androidx.recyclerview.widget.RecyclerView
import com.awonar.app.databinding.AwonarItemDividerBinding
import com.awonar.app.databinding.AwonarItemDividerBlankBinding
import javax.inject.Inject

class DividerViewHolder @Inject constructor(private val binding: AwonarItemDividerBlankBinding) :
    RecyclerView.ViewHolder(binding.root)